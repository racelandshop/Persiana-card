//export const callService = (ev) => ev.callService();
//export const callService = (hass) => hass.callService('cover.open_cover', 'cover.stop_cover', 'cover.close_cover');
export const callService = (ev) => ev.callService();